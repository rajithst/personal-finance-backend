runtime: python312
service: coincraftservice
env: standard
entrypoint: gunicorn -b :$PORT core.wsgi:application

handlers:
  - url: /.*
    script: auto

env_variables:
  ENV: "prod"
  DJANGO_SECRET_NAME: "django_settings"

runtime_config:
  python_version: 3

app_engine_apis: true
